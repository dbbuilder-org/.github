name: Write Linear helper functions
description: Copies shared bash helper functions to /tmp/linear-helpers.sh for use in subsequent steps

runs:
  using: composite
  steps:
    - name: Write helpers
      shell: bash
      run: cp "${{ github.action_path }}/linear-helpers.sh" /tmp/linear-helpers.sh
