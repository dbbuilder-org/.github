name: DEBUG set Linear parent null (temporary)
on:
  workflow_dispatch:
jobs:
  set:
    runs-on: ubuntu-latest
    steps:
      - uses: dbbuilder-org/.github/.github/actions/write-linear-helpers@main
      - env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
        run: |
          source /tmp/linear-helpers.sh
          linear_api 'mutation { issueUpdate(id: "6ffc3098-e743-46e8-a807-911edecdfbff", input: { parentId: null }) { success issue { identifier parent { identifier } } } }' '{}'
